<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Database connection settings -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/supermarket</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">YuG@#2647</property>

        <!-- JDBC connection pool (use a real pool like HikariCP in production) -->
        <property name="hibernate.connection.pool_size">10</property>

        <!-- SQL dialect -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQLDialect</property>

        <!-- Echo all executed SQL to stdout -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>

        <!-- Schema auto-update (use 'validate' in production) -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapped entity classes -->
        <mapping class="JButton1.Customer"/>
        <mapping class="JButton1.Product"/>
        <mapping class="JButton1.Order"/>
    </session-factory>
</hibernate-configuration>
